"use client";
export function Toolbar({saveState,canUndo,canRedo,onUndo,onRedo,onSave,onExportPDF,onExportDOCX,previewOnly,setPreviewOnly}:{saveState:"saved"|"saving"|"unsaved";canUndo:boolean;canRedo:boolean;onUndo:()=>void;onRedo:()=>void;onSave:()=>void;onExportPDF:()=>void;onExportDOCX:()=>void;previewOnly:boolean;setPreviewOnly:(v:boolean)=>void}){
 return <header className="studio-toolbar"><div className="studio-toolbar-brand"><span className="studio-kicker">Makwande Careers</span><strong>CV Studio</strong></div><div className="studio-history-controls"><button disabled={!canUndo} onClick={onUndo}>↶ Undo</button><button disabled={!canRedo} onClick={onRedo}>↷ Redo</button></div><div className={`studio-save-state ${saveState}`}><span className="save-dot"/>{saveState==="saving"?"Saving...":saveState==="unsaved"?"Unsaved changes":"All changes saved"}</div><div className="studio-toolbar-actions"><button onClick={()=>setPreviewOnly(!previewOnly)}>{previewOnly?"Edit CV":"Preview"}</button><button onClick={onSave}>Save now</button><button onClick={onExportPDF}>PDF</button><button className="primary" onClick={onExportDOCX}>DOCX</button></div></header>
}
